Should be able to parse PRIV frames

Allow users calling code from a utf-8 / 16 encoded .js file to watch for events with a symbol in the name.

work out what the minimum required version of nodejs is

if cleanup doesn't work it should always return the original value

implement strictDeepEqual?

test that multiple artworks work

need more tests / more complicated files

we aren't handling files coming off the network correctly