Should be able to parse PRIV frames

Allow users calling code from a utf-8 / 16 encoded .js file to watch for events with a symbol in the name.

(id3) Disconnect from the stream once we have read to the end of the id3 header. Use the header size to determine when to stop reading.

vorbis - use TRACKTOTAL as the second part of the track for the metadata, same with disc

vorbis - do we have a composer?

work out what the minimum required version of nodejs

do we need to call parser.parse?